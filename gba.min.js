// Placeholder do emulador GBA
// Baixar versão real em: https://github.com/endrift/gbajs
class GameBoyAdvance {
  setCanvas(canvas) { this.canvas = canvas; }
  loadRom(buffer, callback) { console.log("ROM carregada (fake)"); callback(); }
  run() { console.log("Rodando jogo (fake)"); }
}
